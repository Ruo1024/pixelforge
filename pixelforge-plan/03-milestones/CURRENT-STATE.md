# PixelForge 当前真实状态

> 本页只回答“现在做到哪里、哪些被真人确认、下一张卡是什么”。仅在任务卡完成、人工结论变化或 go/no-go 决策发生时更新；历史细节留在 reports 与 Git。

## 基线

- 日期：2026-07-11
- origin/main 基线：`a9003ab`；本地 main 基线：`bdfeafc`；长期 Goal 起点：`44a5081`
- 当前施工分支：`codex/pixelforge-full-plan-goal`（仅本地，尚未合并 `main`，尚未 push）
- 已继承本地工程提交：M3/M3.1 `bdfeafc..531aa86`，M4-V1 `44a5081`；2026-07-11 在 Goal 分支复跑 M4-V1/M3.1 门禁通过。
- 当前阶段：M3 与 M3.1 AR-1～AR-3 工程开发完成；M4-V1 工程准备完成但真实 API 与三人 A/B 尚待验。项目所有者授权普通人工门统一延后，并允许按既定规格继续下游工程，因此下一张卡为 M4-1。**当前仅为“暂定工程继续，产品 go/no-go 待最终统一验收”，不是 M3.1 通过、M4-V1 go 或受邀 alpha 签收。**
- 当前自动化：M4-V1 开发态全量门禁 197/197 tests、1515 assertions 通过；新增 OpenAI Provider fixture、异步任务终态、会话密钥清除、错误映射和 provenance 覆盖。既有 orphan / 退出资源警告待后续归因，没有新增用户影响证据。
- 当前候选：本地忽略文件 `pixel/build/PixelForge-0.1.0-alpha.1-macOS.zip`，SHA-256 `c3704e0d2d6d0dcb71e9c9a02b40a4a6657028e4c6b912d08d6ecde2f00baa8d`，64,761,252 bytes；尚未上传或发布。

## 已有人工作证据

- 已通过：批次标记/过滤/焦点切换、Current/Previous/Compare、Focus View；Graph 拖线/删线/吸附、必填端口与失败反馈；近期节点参数编辑与快速添加入口的大部分路径。
- 明确未通过：UX-4 overview 两轮方案均被否决并撤销；当前保留 review/inspect 路径，见 `M3-设计债台账.md`。
- 待统一人工验收：实现基线 `a9003ab` 的“快速添加节点按光标位置落点”并入 Goal 结束时的统一测试；不得写成人工通过。
- 版本级未通过：M2/M3 尚无一轮同时覆盖真实输入、保存重开、强杀恢复、候选构建与陌生测试者的完整签收。

## 当前已知 Alpha 阻断

1. AR-1 数据安全工程路径已补齐，尚待统一人工验收 New / Open / Quit、强杀恢复与无写权限失败。
2. AR-2 首次导入、任务状态与导出反馈工程路径已补齐，尚待统一人工执行完整真实素材旅程。
3. 当前主平台候选构建、精确包审计和干净用户配置 agent 冒烟已完成；3 名未参与开发的目标用户测试为硬出口，当前仍为未通过。
4. M4-V1 尚无可用 OpenAI key 的真实调用证据，也尚未执行三人 A/B；当前只能称工程准备完成，不能称 M4-V1 完成或作出 go/no-go。

AR-1/AR-2 的 Computer Use 检查只算 agent 实机冒烟；所有用户工作流仍保持“待统一人工验收”，不得写成人工通过。

## 固定执行顺序

1. `M3.1-alpha-readiness.md` 的 AR-1 → AR-3 工程卡已完成并各自本地提交；人工状态保持“待统一人工验收”。
2. 路线例外（2026-07-11）：项目所有者暂不测试并要求继续原规划，允许先完成 M4-V1 工程准备；M3.1 原有出口条件不变、不得倒填通过。
3. 获得测试 key 后，补运行 M4-V1 的真实单 Provider 固定任务；最终统一验收仍需三名测试者完成集成生成与外部生成导入 A/B。
4. Goal 路线例外（2026-07-11）：在不倒填 `go` 的前提下，按 M4-1 → M4-5 继续模块化工程施工；所有下游状态均保持“待最终统一验收”，若最终 no-go 可整体不合并 Goal 分支。

## 明确不在当前主线

三平台同步发布、签名公证、自动更新、完整欢迎页、无障碍/本地化、LOD 重做、完整 M4、M5–M7，均不进入 M3.1。
