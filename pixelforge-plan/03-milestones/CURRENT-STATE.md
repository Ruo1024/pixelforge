# PixelForge 当前真实状态

> 本页只回答“现在做到哪里、哪些被真人确认、下一张卡是什么”。仅在任务卡完成、人工结论变化或 go/no-go 决策发生时更新；历史细节留在 reports 与 Git。

## 基线

- 日期：2026-07-10
- 最近验证的实现基线：`main` / `a9003ab`（计划文档提交不会改变此实现审计基线）
- 当前阶段：M3 最小节点闭环收口；内部原型，**不是受邀 alpha，也不是公开发布版**。
- 当前自动化：`verify_m3_ux7.sh` 实跑 177/177 tests、1373 assertions 通过；仍有 orphan / 退出资源警告待后续归因，不在没有用户影响证据时阻塞当前卡。

## 已有人工作证据

- 已通过：批次标记/过滤/焦点切换、Current/Previous/Compare、Focus View；Graph 拖线/删线/吸附、必填端口与失败反馈；近期节点参数编辑与快速添加入口的大部分路径。
- 明确未通过：UX-4 overview 两轮方案均被否决并撤销；当前保留 review/inspect 路径，见 `M3-设计债台账.md`。
- 待验：实现基线 `a9003ab` 的“快速添加节点按光标位置落点”尚无用户回执；不得写成人工通过。
- 版本级未通过：M2/M3 尚无一轮同时覆盖真实输入、保存重开、强杀恢复、候选构建与陌生测试者的完整签收。

## 当前已知 Alpha 阻断

1. New / Open / Quit 缺少未保存保护；恢复提示时序与恢复后保存语义需验证和修正。
2. 文件导入会受弹窗关闭时鼠标位置影响；真实大图可能落在难以发现的位置。
3. 清洗预览/任务状态可能长期停留在 queued；文件失败主要依赖日志，缺少可执行反馈。
4. 尚无当前主平台候选构建与陌生测试者最小旅程证据。

## 固定执行顺序

1. 完成实现基线 `a9003ab` 的人工签收，并关闭 M3 当前 G-4 收口；同时完成状态校准和 UX-4 设计债登记，不另立纯文档施工卡。
2. 执行 `M3.1-alpha-readiness.md` 的 AR-1 → AR-3；期间不追加节点便利性小卡。
3. M3.1 通过后可发当前主平台受邀 v0.1 alpha。
4. 再执行 `M4-ai-providers.md` 的 M4-V1；只有 go 决策才能解冻完整 M4。

## 明确不在当前主线

三平台同步发布、签名公证、自动更新、完整欢迎页、无障碍/本地化、LOD 重做、完整 M4、M5–M7，均不进入 M3.1。
