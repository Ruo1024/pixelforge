# M2.4 缩放终因追加修复完成报告

完成日期：2026-06-17
对应反馈：无缩放 2K 屏 UI 偏小；macOS 带缩放内屏 UI 过大裁切；拉大 Debug 窗口后实际工作区周围出现黑色盲区。

---

## 1. 结论

本次 M2.4 缩放收口已完成并通过 `pixel/scripts/verify_m2_4.sh`。

终极原因不是单个 Control 或 Godot 弹窗，而是 M2.4 前一版把同一个 `_interface_scale` 同时用于三件事：

1. `Window.content_scale_factor` 的 UI 视觉可读倍率。
2. 启动窗口默认尺寸 / 最小尺寸的窗口像素倍率。
3. macOS 上 `screen_get_usable_rect()` 与 `Window.size` 之间的点/像素换算倍率。

这会在两类屏幕上反向出错：

- 无缩放 2K 外屏：`screen_get_scale()` 为 1.0，旧阈值又只从 3200/1800 才抬档，导致 `2560x1440 -> 1.0`，界面偏小。
- macOS 缩放内屏：Retina/缩放信号被直接当成 UI 视觉 2.0，右侧检查器和 Debug 运行工具栏叠加后很容易过大裁切；同时窗口几何也跟同一倍率耦合，放大窗口不能从根上解决。

本次改为拆分：

- `content_scale_factor`：面向人眼可读性的 UI 视觉档，自动值为 1.0 / 1.25 / 1.5 / 2.0。
- `window_pixel_scale`：屏幕/Retina backing 像素档，只用于窗口几何和 mac 点/像素换算。
- `window_geometry_scale`：启动默认窗口尺寸使用 `max(content_scale_factor, window_pixel_scale)`，保证 Retina 内屏不会因为 UI 视觉降到 1.25 而打开成太小窗口。

官方依据：

- Godot `Window.content_scale_factor` 是窗口内容基础缩放；`content_scale_size` 是虚拟像素，`Window.size` 是窗口物理像素：https://docs.godotengine.org/en/stable/classes/class_window.html
- Godot `DisplayServer.screen_get_scale()` 返回屏幕缩放因子；`screen_get_size()` / `screen_get_usable_rect()` 文档口径为像素：https://docs.godotengine.org/en/stable/classes/class_displayserver.html
- Godot 非游戏应用推荐 `disabled` stretch mode，并在运行时用 `root.content_scale_factor` 配置 UI 缩放；Windows 等平台需要按分辨率猜测并允许用户覆盖：https://docs.godotengine.org/en/stable/tutorials/rendering/multiple_resolutions.html

---

## 2. 修改文件

| 路径 | 作用 |
|---|---|
| `pixel/ui/shell/interface_scale_policy.gd` | 新增 2K 可读档、mac 紧凑 Retina 可读档、`window_pixel_scale_from_snapshot()`；日志快照增加 `screen_size`。 |
| `pixel/ui/shell/window_scale_policy.gd` | 新增 `effective_window_geometry_scale()`；mac 点口径只在明确小尺寸时转换，避免物理像素 rect 被二次乘。 |
| `pixel/ui/shell/main.gd` | 启动与 live re-scale 使用同一份屏幕快照；记录并传递 `_window_pixel_scale`；日志输出两个缩放职责。 |
| `pixel/ui/shell/scale_audit.gd` | `--scale-audit` 增加 `window_pixel_scale` 字段。 |
| `pixel/tests/smoke/test_main_window_ui.gd` | 固定 2K 外屏、mac Retina 点口径、mac 3024x1964 紧凑内屏、窗口换算职责拆分的回归测试。 |
| `pixel/project.godot` | 显式启用 `window/dpi/allow_hidpi=true`，保证从 Godot 编辑器 Debug 运行时仍保持 DPI-aware。 |
| `pixel/CHANGELOG.md` | 追加 M2.4 终因修复摘要。 |

完整代码 diff：

- `pixelforge-plan/03-milestones/reports/M2.4_缩放终因追加修复.diff`
- `pixelforge-plan/03-milestones/reports/M2.4_窗口填充缩放追加修复.diff`
- `pixelforge-plan/03-milestones/reports/M2.4_缩放体验最终合并代码.diff`

---

## 3. 行为变更

自动档位调整：

- `2560x1440 / scale=1.0 -> content_scale_factor=1.25`
- `3840x2160 / scale=1.0 -> content_scale_factor=1.5`
- `5120x3140 / scale=1.0 -> content_scale_factor=2.0`
- macOS 点口径 `1244x778` / `1334x834` / `1440x900 -> content_scale_factor=1.25`
- macOS Retina 内屏 `3024x1964 / reported_scale=2.0 -> content_scale_factor=1.25`，但 `window_pixel_scale=2.0`

窗口几何调整：

- 启动窗口默认尺寸使用 `window_geometry_scale=max(content_scale_factor, window_pixel_scale)`。
- macOS `usable_rect` 只有在 `<= 2200x1400` 且 `window_pixel_scale>1` 时按点口径转换；`3024x1964` 这类已经像物理像素的可用区域不再二次乘。
- `Window defaults applied`、`Interface scale resolved`、`Interface scale reapplied`、`Scale audit` 日志都能区分 UI 视觉档和窗口像素档。

窗口填充缩放：

- `Window.content_scale_mode` 改为 `CANVAS_ITEMS`，`content_scale_aspect` 改为 `EXPAND`，项目设置同步为 `window/stretch/mode="canvas_items"`、`aspect="expand"`、`scale_mode="fractional"`。
- 启动时先应用窗口默认尺寸，再把当前 `Window.size` 捕获为 `content_scale_size`。初始显示不额外放大；用户拉大 Debug/运行窗口后，引擎按新窗口尺寸自动放大并填满外圈盲区。
- 画布物理像素补偿不再只看 `content_scale_factor`，而是读取 `content_scale_factor × window stretch scale`。这样工作区填满后，画布吸附与设备整数倍率仍按最终物理倍率计算。
- `CanvasScalePolicy.compute_window_stretch_scale()` 与 `resolve_viewport_scale_factor()` 已覆盖 `expand` 模式的缩放倍率计算，并由 `test_canvas_viewport_scale_includes_window_stretch()` 固定。

---

## 4. 验证记录

已执行：

```text
PATH="$PWD/.godot/gdtoolkit-venv/bin:$PATH" gdformat ui/shell/interface_scale_policy.gd ui/shell/main.gd ui/canvas/canvas_scale_policy.gd ui/canvas/infinite_canvas.gd tests/smoke/test_main_window_ui.gd tests/smoke/test_infinite_canvas.gd
./scripts/lint.sh
./scripts/check_ui_scaling.sh
git diff --check -- CHANGELOG.md project.godot ui/shell/interface_scale_policy.gd ui/shell/main.gd ui/canvas/canvas_scale_policy.gd ui/canvas/infinite_canvas.gd tests/smoke/test_main_window_ui.gd tests/smoke/test_infinite_canvas.gd
./scripts/run_tests.sh
./scripts/verify_m2_4.sh
source ./scripts/_godot_path.sh && GODOT_BIN=$(find_godot) && prepare_godot_env && "$GODOT_BIN" --path . --quit-after 3 --scale-audit
```

结果：

- `gdformat`：最终复跑为 0 files reformatted, 6 files left unchanged；首次格式化时仅 `ui/canvas/canvas_scale_policy.gd` 被自动整理。
- `lint.sh`：87 files would be left unchanged；Success: no problems found。
- `check_ui_scaling.sh`：ok。
- `run_tests.sh`：115/115 passed，902 asserts。
- `verify_m2_4.sh`：ok。
- 图形模式 `--scale-audit`：在 macOS Retina 环境记录 `applied_F=1.25`、`window_pixel_scale=2.0`、`viewport_scale_factor=1.25`，初始窗口按当前窗口尺寸捕获为 stretch 基准；拉大窗口后的真机填充效果需用户复测截图确认。

已知非本次新增现象：

- GUT 仍报告 1 个既有 orphan：`test_cleanup_batch_performance.gd` 外部 `error_tracker.gd`。
- Godot 退出时仍报告资源 warning：`3 resources still in use at exit`。
- 导出模板未安装时，`check_export_templates.sh` 按既有逻辑降级为 headless startup 门控；`verify_m2_4.sh` 仍退出 0。

---

## 5. DoD 核查

| 项 | 核查内容 | 状态 | 证据/路径 |
|---|---|---|---|
| 代码规范 | gdlint/gdformat 零告警 | 通过 | `./scripts/lint.sh` |
| 自动测试 | 受影响验收已转自动化并通过 | 通过 | `test_main_window_ui.gd` 覆盖 2K、Retina、窗口换算；`test_infinite_canvas.gd` 覆盖窗口拉伸倍率；`run_tests.sh` 115/115 |
| 手动测试 | 真机视觉签收 | 延期登记 | 仍需 Ruo 在 mac 内屏 + 2K 外屏 + 拉大 Debug 窗口后复测截图；本轮已补 `--scale-audit` 字段 |
| 契约同步 | 未影响 `02-contracts/` | 不适用 | 仅 UI/窗口策略 |
| TODO | 无新增 TODO/FIXME/HACK | 通过 | lint 与人工 diff 检查 |
| 性能预算 | 无性能预算变化 | 不适用 | 纯启动/缩放策略 |
| 跨平台 | headless 自动化覆盖；真机待复测 | 延期登记 | M2.4 真机矩阵仍需用户在实际设备回填 |
| 出口门控 | 本地 M2.4 门控通过 | 通过 | `./scripts/verify_m2_4.sh` |

---

## 6. 真机复测建议

请在同一 profile 下启动，确认日志中出现类似字段：

```text
Interface scale resolved | {
  "applied_F": 1.25,
  "window_pixel_scale": 2.0,
  "reported_screen_scale": 2.0,
  "usable_rect": [3024, 1964]
}
```

重点看两项：

1. macOS 带缩放内屏：右侧检查器不再过大裁切，窗口可用区域能容纳 Apply/Report 区域滚动访问。
2. 无缩放 2K 外屏：`applied_F` 应为 1.25，文字/控件不再按 1.0 显得过小。
3. 拉大 Debug/运行窗口：PixelForge 根工作区和画布应随窗口放大并覆盖外圈黑色盲区。

---

## 7. Diff 记录优先级说明

按本轮要求，报告记录优先级最高。本报告同时保留三份 diff：

1. `M2.4_缩放终因追加修复.diff`：上一轮针对 2K 偏小 / Retina 过大裁切的完整代码 diff。
2. `M2.4_窗口填充缩放追加修复.diff`：本轮针对 Debug 窗口拉大后黑色盲区的完整代码 diff。
3. `M2.4_缩放体验最终合并代码.diff`：从提交 `e35c4e8 M2.4 harden scaling experience` 到当前工作树的合并代码 diff，便于一次性审阅本轮对话中的所有缩放代码修复。
