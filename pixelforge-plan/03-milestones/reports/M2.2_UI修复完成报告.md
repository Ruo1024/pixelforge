# M2.2 UI 修复完成报告

日期：2026-06-16

## 1. 完成结论

M2.2 已完成一轮可验证修复：高分屏界面缩放不再被启动窗口尺寸反向钳小，清洗台底部 Apply/Cancel 固定可达，批次卡尺寸与缩略图清晰度提升，多图导入/创建批次后自动取景，滚轮缩放加入中间档与限频，导出空选区/失败时给显眼提示，描边对低 alpha 抗锯齿边缘做二值化以减少噪点，首启引导改为关闭后再写 seen 标记。

本轮按 M2.2 指导选择 P1-3 的 A 路径：暂隐藏 W/M/L 工具入口和快捷键，保留底层工具代码与测试，避免用户触发尚未接入 Matte/Slice/Outline 的假工作流。

完整代码差异见同目录：

```text
pixelforge-plan/03-milestones/reports/M2.2_UI修复完整代码.diff
```

## 2. 实现清单

| 任务 | 状态 | 主要文件 |
|---|---|---|
| P0-1 界面缩放钳制 | 完成 | `pixel/ui/shell/main.gd`、`pixel/tests/smoke/test_main_window_ui.gd` |
| P0-2 清洗台可滚动/Apply 可达 | 完成 | `pixel/ui/inspector/cleanup_inspector.gd`、`pixel/tests/smoke/test_main_window_ui.gd` |
| P0-3 批次卡可辨识/导入后取景 | 完成 | `pixel/ui/canvas/canvas_batch_card.gd`、`pixel/ui/shell/m2_1_ui_controller.gd`、`pixel/tests/unit/test_canvas_batch_card.gd` |
| P0-4 滚轮缩放过冲 | 完成 | `pixel/ui/canvas/infinite_canvas.gd`、`pixel/tests/smoke/test_infinite_canvas.gd` |
| P0-5 Export PNG 空选区提示 | 完成 | `pixel/ui/shell/main.gd`、`pixel/ui/shell/strings.gd` |
| P1-1 Outline 噪点 | 完成 | `pixel/core/pixel/outliner.gd`、`pixel/tests/unit/test_pixel_outliner.gd` |
| P1-2 首启引导 | 完成 | `pixel/ui/dialogs/onboarding.gd`、`pixel/ui/shell/m2_1_ui_controller.gd`、`pixel/ui/shell/strings.gd` |
| P1-3 W/M/L 决策 | 完成 A 路径 | `pixel/ui/shell/m2_1_ui_controller.gd`、`pixel/ui/shell/strings.gd` |
| Changelog | 完成 | `pixel/CHANGELOG.md` |

## 3. 验证结果

已执行：

```bash
cd /Users/ruo/Desktop/pixelforge/pixel
./scripts/lint.sh
./scripts/run_tests.sh
./scripts/verify_m2.sh
```

结果：

- `gdformat` / `gdlint`：通过。
- GUT：103 tests / 103 passing / 857 asserts。
- `verify_m2.sh`：`verify_m2: ok`。
- Export templates：本机未安装 Godot 4.6.3 export templates，脚本按 M0 本地门控策略降级为 headless startup 检查并通过。
- 已确认此前一次测试中 `test_infinite_canvas.gd` 因类型推断解析失败被 GUT 跳过；已修正并重新运行，第二次结果包含该脚本 9/9 通过。
- 仍有既有的 GUT 结束阶段 1 个 orphan 与 Godot exit resource warning；退出码为 0，测试套件判定通过。

## 4. DoD 核查

| 项 | 核查内容 | 状态 | 证据/路径 |
|---|---|---|---|
| 代码规范 | gdlint/gdformat 零告警 | 通过 | `./scripts/lint.sh` |
| 自动测试 | 卡内验收标准已转自动化并通过 | 通过 | `./scripts/run_tests.sh`，103/103 |
| 手动测试 | 真机窗口/Windows 150% 复测 | 延期登记 | 需用户在 macOS 实窗与 Windows 真机复核 |
| 契约同步 | 影响契约的改动已更新 `02-contracts/` | 不适用 | 本轮未改项目/图/Provider/插件契约 |
| TODO | 一方代码无无主 `TODO/FIXME/HACK` | 通过 | lint 与人工检查 |
| 性能预算 | 滚轮/布局/描边回归无新增性能卡 | 不适用 | 自动测试覆盖行为，不登记性能数字 |
| 跨平台 | macOS headless 自动化已跑；Windows 未跑 | 延期登记 | Windows 150% 需后续真机 |
| 出口门控 | 本地 agent 验证 | 通过 | `./scripts/verify_m2.sh`，`verify_m2: ok` |

## 5. 延后项

- W/M/L 完整修复延期：需要把选区真正喂给 Matte/Slice/Outline，并补真实轮廓蚂蚁线、光标状态、撤销和集成测试。
- P2 批次打磨延期：批次 Matte/Outline 参数化、批次进度百分比、New/File 菜单收敛、Windows 150% 手测与录屏。
- 人工复测延期：M2.2 指导 §7 的清洗台全流程、批次整批菜单与撤销、Export PNG、清洗预览随参数变化仍需真实窗口手测确认。

## 6. Diff 范围

完整 diff 文件只包含本轮 touched 文件：

```text
pixel/CHANGELOG.md
pixel/core/pixel/outliner.gd
pixel/tests/smoke/test_infinite_canvas.gd
pixel/tests/smoke/test_main_window_ui.gd
pixel/tests/unit/test_canvas_batch_card.gd
pixel/tests/unit/test_pixel_outliner.gd
pixel/ui/canvas/canvas_batch_card.gd
pixel/ui/canvas/infinite_canvas.gd
pixel/ui/dialogs/onboarding.gd
pixel/ui/inspector/cleanup_inspector.gd
pixel/ui/shell/m2_1_ui_controller.gd
pixel/ui/shell/main.gd
pixel/ui/shell/strings.gd
```

接手前已存在的计划/契约/研究文档改动未纳入本报告 diff，保持原样等待用户或对应 agent 处理。
